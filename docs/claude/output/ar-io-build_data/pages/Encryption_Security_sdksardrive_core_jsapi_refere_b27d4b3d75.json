{
  "title": "Encryption & Security (/sdks/ardrive-core-js/(api-reference)/encryption-security)",
  "content": "#### Manual Encryption/Decryption",
  "code_samples": [
    {
      "code": "// Derive drive key from password\nconst driveKey = await deriveDriveKey(\n  'myPassword',\n  driveId.toString(),\n  JSON.stringify(wallet.getPrivateKey())\n);\n\n// File keys are automatically derived from drive keys\nconst fileKey = await deriveFileKey(driveKey, fileId);",
      "language": "typescript"
    },
    {
      "code": "// Encrypt data\nconst { cipher, cipherIV } = await driveEncrypt(driveKey, data);\n\n// Decrypt data\nconst decrypted = await driveDecrypt(cipherIV, driveKey, cipher);",
      "language": "typescript"
    }
  ],
  "headings": [],
  "url": "llms-txt#encryption-&-security-(/sdks/ardrive-core-js/(api-reference)/encryption-security)",
  "links": []
}